#!/usr/bin/env node

// TODO: update checker here

var Cli = require('../lib/cli');

var cli = Cli();

cli.run(process.argv, function () {
  
  console.log('Superstatic started on port ' + cli.get('port'));
});